---
description: 云游平台 UI 唯一入口与构建流程
alwaysApply: true
---

# 云游平台入口

- **唯一 UI 入口**：`layout-preview.html`（加载 `platform-main.js` + `layout-preview.css`）
- **不要**把 `index.html` 当作主界面维护或验证目标；功能改动请改 `platform/parts/*.js`，再运行 `platform/build.ps1`
- 游戏 iframe 桥接在 `game.html`；静态购买目录为 `buy-catalog.json`
